# Electron 22 arrastra @types/node 16 (runtime Node 16), pero vite 5 declara un peer
# opcional de @types/node >=18. Es solo un peerOptional de tipos; ignorarlo mantiene los
# tipos alineados al runtime real (Node 16) sin romper la resolución en CI (`npm install`).
legacy-peer-deps=true
